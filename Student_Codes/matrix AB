def matrix(A, B, value = 0):
    m = len(A)    
    k = len(A[0])    
    n = len(B[0])    

 
    AB = [[value for _ in range(n)] for _ in range(m)]

    for num1 in range(m):
        for num2 in range(n):
            for num3 in range(k):
                AB[num1][num2] += A[num1][num3] * B[num3][num2]

    return AB

A = [[1, 2], 
     [3, 4], 
     [5, 6]] 

B = [[7, 8, 9], 
     [10, 11, 12]] 

AB = matrix(A, B)

print("Solution for AB")
for row in AB:
    print(row)
